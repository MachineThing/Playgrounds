#include <iostream> // Input Output STREAM library
using namespace std; // Use the STandarD namespace

int main() { // Entry point
  const int numa = 17; // constant intiger variable
  const int numb = 42; // constant intiger variable
  std::cout << numa << " plus " << numb << " equals " << numa+numb << "!" << endl;
  return 0; // Return 0 as the exit code
}
